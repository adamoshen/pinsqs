# Deprecated functions

**\[deprecated\]**

Use
[`pin_qwrite()`](https://adamoshen.github.io/pinsqs/reference/read-write.md)
instead of `pin_qsave()`.

## Usage

``` r
pin_qsave(board, x, name, nthreads = NULL, ...)
```
